import { renderHook, act } from '@testing-library/react-hooks'; import useXxx from '@/hooks/useXxx'; import { mockApi } from '../utils/test-utils'; describe('useXxx Hook', () => { beforeEach(() => { mockApi(); }); it('returns loading=true initially', () => { const { result } = renderHook(() => useXxx()); expect(result.current.loading).toBe(true); }); it('returns data on successful fetch', async () => { mockApi.mockResolvedValueOnce({ data: [{ id: 1, name: 'Item 1' }] }); const { result, waitForNextUpdate } = renderHook(() => useXxx()); await waitForNextUpdate(); expect(result.current.data).toEqual([{ id: 1, name: 'Item 1' }]); }); it('returns error on failed fetch', async () => { mockApi.mockRejectedValueOnce(new Error('Fetch Error')); const { result, waitForNextUpdate } = renderHook(() => useXxx()); await waitForNextUpdate(); expect(result.current.error).toEqual('Fetch Error'); }); it('refetch works after error', async () => { mockApi.mockRejectedValueOnce(new Error('Fetch Error')).mockResolvedValueOnce({ data: [{ id: 1, name: 'Item 1' }] }); const { result, waitForNextUpdate } = renderHook(() => useXxx()); await waitForNextUpdate(); act(() => { result.current.refetch(); }); await waitForNextUpdate(); expect(result.current.data).toEqual([{ id: 1, name: 'Item 1' }]); }); it('handles empty response', async () => { mockApi.mockResolvedValueOnce({ data: [] }); const { result, waitForNextUpdate } = renderHook(() => useXxx()); await waitForNextUpdate(); expect(result.current.data).toEqual([]); }); });