import React from 'react'; import { fireEvent, render } from '@testing-library/react-native'; import Login from '@/app/(auth)/login'; import { mockApi, renderWithProviders } from '../utils/test-utils'; describe('Login Screen', () => { beforeEach(() => { mockApi(); }); it('renders email and password inputs', () => { const { getByPlaceholderText } = renderWithProviders(<Login />); expect(getByPlaceholderText('Email')).toBeTruthy(); expect(getByPlaceholderText('Password')).toBeTruthy(); }); it('shows validation errors for empty fields on submit', () => { const { getByText, getByRole } = renderWithProviders(<Login />); fireEvent.press(getByRole('button', { name: /login/i })); expect(getByText('Email is required')).toBeTruthy(); expect(getByText('Password is required')).toBeTruthy(); }); it('shows validation error for invalid email format', () => { const { getByText, getByPlaceholderText, getByRole } = renderWithProviders(<Login />); fireEvent.changeText(getByPlaceholderText('Email'), 'invalid-email'); fireEvent.press(getByRole('button', { name: /login/i })); expect(getByText('Invalid email format')).toBeTruthy(); }); it('calls login() with credentials on valid submit', async () => { const { getByPlaceholderText, getByRole } = renderWithProviders(<Login />); fireEvent.changeText(getByPlaceholderText('Email'), 'user@example.com'); fireEvent.changeText(getByPlaceholderText('Password'), 'Password123!'); fireEvent.press(getByRole('button', { name: /login/i })); await waitFor(() => expect(mockApi).toHaveBeenCalledWith(expect.objectContaining({ email: 'user@example.com', password: 'Password123!' }))); }); it('shows loading state during login', () => { const { getByRole, getByTestId } = renderWithProviders(<Login />); fireEvent.press(getByRole('button', { name: /login/i })); expect(getByTestId('spinner')).toBeTruthy(); }); it('shows error message when login fails (401)', async () => { mockApi.mockRejectedValueOnce({ response: { status: 401 } }); const { getByText, getByRole } = renderWithProviders(<Login />); fireEvent.press(getByRole('button', { name: /login/i })); await waitFor(() => expect(getByText('Invalid credentials')).toBeTruthy()); }); it('shows error message when network is down', async () => { mockApi.mockRejectedValueOnce(new Error('Network Error')); const { getByText, getByRole } = renderWithProviders(<Login />); fireEvent.press(getByRole('button', { name: /login/i })); await waitFor(() => expect(getByText('Network Error')).toBeTruthy()); }); it('navigates to main app on successful login', async () => { const { getByRole } = renderWithProviders(<Login />); fireEvent.press(getByRole('button', { name: /login/i })); await waitFor(() => expect(useRouter().push).toHaveBeenCalledWith('/main')); }); it('navigates to signup screen when link tapped', () => { const { getByText } = renderWithProviders(<Login />); fireEvent.press(getByText('Sign up')); expect(useRouter().push).toHaveBeenCalledWith('/signup'); }); });