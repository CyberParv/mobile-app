export const createUser = (id = 1) => ({ id, name: `User ${id}`, email: `user${id}@example.com`, password: 'Password123!' }); export const createWorkout = (id = 1) => ({ id, title: `Workout ${id}`, duration: 60 }); export const createEntity = (id = 1) => ({ id, name: `Entity ${id}` });