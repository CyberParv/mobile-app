import React from 'react'; import { render } from '@testing-library/react-native'; import { AuthProvider } from '@/providers/AuthProvider'; import { ToastProvider } from '@/providers/ToastProvider'; import { SafeAreaProvider } from 'react-native-safe-area-context'; import axios from 'axios'; export const renderWithProviders = (component, { overrideAuth } = {}) => { return render(<AuthProvider overrideAuth={overrideAuth}><ToastProvider><SafeAreaProvider>{component}</SafeAreaProvider></ToastProvider></AuthProvider>); }; export const mockApi = (response, status = 200) => { axios.interceptors.request.use(config => { config.adapter = async () => ({ data: response, status, config, headers: {}, statusText: '', request: {} }); return config; }); }; export const createMockUser = (id = 1) => ({ id, name: `User ${id}`, email: `user${id}@example.com` }); export const createMockEntity = (id = 1) => ({ id, name: `Entity ${id}` });