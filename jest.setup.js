import mockAsyncStorage from '@react-native-async-storage/async-storage/jest/async-storage-mock'; import * as SecureStore from 'expo-secure-store'; import { useRouter, useLocalSearchParams, Link } from 'expo-router'; import Reanimated from 'react-native-reanimated/mock'; import 'react-native-gesture-handler/jestSetup'; import fetchMock from 'jest-fetch-mock'; jest.mock('@react-native-async-storage/async-storage', () => mockAsyncStorage); jest.mock('expo-secure-store', () => ({ getItemAsync: jest.fn(), setItemAsync: jest.fn(), deleteItemAsync: jest.fn() })); jest.mock('expo-router', () => ({ useRouter: jest.fn(), useLocalSearchParams: jest.fn(), Link: jest.fn() })); jest.mock('react-native-reanimated', () => Reanimated); fetchMock.enableMocks(); console.warn = jest.fn();